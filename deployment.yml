apiVersion: apps/v1
kind: Deployment

metadata:
  name: food-for-all

spec:
  replicas: 2

  selector:
    matchLabels:
      app: food-for-all

  template:
    metadata:
      labels:
        app: food-for-all

    spec:
      containers:
        - name: food-for-all

          image: priyanshu8948/food-for-all

          ports:
            - containerPort: 5000

          env:
            - name: MONGODB_URI
              value: "mongodb+srv://priyanshu_sharma:priyanshu9026707552@cluster0.p2suhpf.mongodb.net/foodforall?retryWrites=true&w=majority&appName=Cluster0"

            - name: JWT_SECRET
              value: "foodforall_super_secret_key_2026"